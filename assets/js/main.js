(() => { const btn=document.querySelector('.menu-toggle'),nav=document.querySelector('#main-nav');if(btn)btn.addEventListener('click',()=>btn.setAttribute('aria-expanded',String(btn.getAttribute('aria-expanded')!=='true')));const clock=document.querySelector('[data-shop-clock]'),tick=()=>{if(clock)clock.textContent=new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Makassar',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date())};tick();setInterval(tick,1000);document.querySelectorAll('body *').forEach(el=>{if(el.children.length===0){el.textContent=el.textContent.replace(/Ellen[’']s Florist/g,'Ellens Florist').replace(/09:00–20:00(?! WITA)/g,'09:00–20:00 WITA')}});const slides=[...document.querySelectorAll('[data-testimonial]')];if(slides.length>1){let i=0;setInterval(()=>{slides[i].hidden=true;i=(i+1)%slides.length;slides[i].hidden=false},5500)}})();
