(() => { const btn=document.querySelector('.menu-toggle'), nav=document.querySelector('#main-nav'); if(btn) btn.addEventListener('click',()=>btn.setAttribute('aria-expanded',String(btn.getAttribute('aria-expanded')!=='true'))); const clock=document.querySelector('[data-shop-clock]'); const tick=()=>{if(clock) clock.textContent=new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Singapore',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date())};tick();setInterval(tick,1000); })();
